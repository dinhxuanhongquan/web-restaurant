server:
  port: 8000
  servlet:
    context-path: /restaurant
    
spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/web_restaurant}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS:090203Qu@n}
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
